mutation Login($creds: UserLoginInput!) {
  login(creds: $creds) {
    user {
      id
      username
      email
      createdAt
      updatedAt
    }
    errors {
      field
      message
    }
  }
}
