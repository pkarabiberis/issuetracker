mutation Register($creds: UserRegisterInput!) {
  register(creds: $creds) {
    user {
      id
      username
      email
      createdAt
      updatedAt
    }
    errors {
      field
      message
    }
  }
}
